node_modules
dist
.astro
.wrangler
.env
.env.local
.env.*.local
.DS_Store
*.log

# Override global gitignore — lockfile must be committed for
# reproducible builds on Cloudflare Workers Builds.
!pnpm-lock.yaml
